//! Codigo para filtrar por content en este caso
// Note.find({ content: "JavaScript" }).then((result) => {
//   result.forEach((note) => {
//     console.log(note);
//   });

//   mongoose.connection.close();
// });
